---
title: 'Terraform patterns'
layout: ../../layouts/Layout.astro
description: 'Reusable Terraform modules for target Databricks foundations.'
---

import Callout from '../../components/Callout.tsx';
import CodeBlock from '../../components/CodeBlock.tsx';
import Checklist from '../../components/Checklist.tsx';
import ArchifyDiagram from '../../components/ArchifyDiagram.tsx';

# Terraform patterns

Use infrastructure as code to create repeatable target foundations. These patterns are cloud-agnostic in structure but use cloud-specific resources.

## Why this matters

Hand-writing target-cloud Terraform from scratch reproduces whatever the source workspace happens to look like today, drift and all. Generating a starting point from the source workspace's actual configuration — rather than only writing target resources by hand — catches settings teams forgot they had.

## Applicability

Use these patterns once the target cloud, region, and Unity Catalog topology are decided (see [Unity Catalog strategy](/governance/unity-catalog-strategy)). Not a substitute for the exporter step below when a source workspace configuration already exists and needs to be captured before it's decommissioned.

## Generating Terraform from source state

<Callout client:visible variant="tip" title="Export before you hand-write">
  Before writing target Terraform by hand, use the [Databricks Terraform exporter](https://registry.terraform.io/providers/databricks/databricks/latest/docs/guides/experimental-exporter) (`databricks exporter`, an experimental feature of the Databricks Terraform provider) to generate a baseline configuration from the *source* workspace. It captures clusters, jobs, notebooks, cluster policies, and (with scope flags) Unity Catalog objects as importable Terraform — giving you a real inventory of what exists today instead of relying on tribal knowledge of what should exist. Adapt the generated resources for the target cloud rather than starting from a blank module.
</Callout>

<CodeBlock client:visible language="bash" code={`# The exporter ships inside the databricks/databricks Terraform provider plugin, not as a
# separate CLI. Authenticate via DATABRICKS_HOST / DATABRICKS_TOKEN env vars (or let it
# prompt interactively), then run the provider binary from your Terraform plugin cache:
export DATABRICKS_HOST=<SOURCE_WORKSPACE_URL>
export DATABRICKS_TOKEN=<TOKEN>

terraform-provider-databricks exporter \\
  -skip-interactive \\
  -listing=compute,jobs,notebooks,policies \\
  -directory ./exported`} />

This is distinct from [UCX-assisted migration](/governance/ucx-strategy): the exporter captures workspace-level object configuration as IaC; UCX assesses and executes the Hive-metastore-to-Unity-Catalog upgrade. Large estates typically use both — UCX for the governance upgrade, the exporter for capturing workspace object configuration as a Terraform starting point.

<Callout client:visible variant="warning" title="Four exporter behaviors that change a bulk-job migration plan">
  - **`-services` organizes output, it doesn't split it into per-team modules.** No flag does that — generate the regex/name-list per wave yourself.
  - **Jobs deployed via Asset Bundles aren't exported at all.** Re-deploy their bundle against the target instead.
  - **`-incremental` doesn't work for jobs or clusters** — only notebooks/SQL objects have reliable timestamps, so a re-export is always full.
  - **`-targetCloud`/`-nodeTypeMappingFile`** translate node types automatically (~99.5% coverage per Databricks), but attributes with no equivalent (`instance_profile_arn`) are silently omitted — diff the plan, don't just apply it.
</Callout>

<Callout client:visible variant="tip" title="For jobs/bundles specifically, Asset Bundle custom templates beat a giant exporter run">
  `databricks bundle init --config-file answers-<team>.json` is fully non-interactive — no manual prompts. For a large estate, emit one JSON per team/domain from your job inventory and invoke `bundle init` once per file, rather than one exporter run over everything. See [Asset Bundle custom templates](https://docs.databricks.com/aws/en/dev-tools/bundles/template-tutorial).
</Callout>

## Factory-pattern modules — what's real, what's stale

<ArchifyDiagram
  client:visible
  name="terraform-patterns.workflow"
  type="workflow"
  title="Verify before you depend: commit date, then license, then source"
  caption="Two traps pass a casual glance: a repo that looks active from its issue/PR/star feed but hasn't merged to main in over a year, and a well-shaped module whose license forbids the fork everyone assumes is allowed. Automation is the rare outcome — most candidates end at 'cite with caveats'."
/>

<Callout client:visible variant="warning" title="Verify staleness yourself before depending on any of these">
  GitHub's `updated_at` bumps on stars/comments — not proof of a code push. Dates below are actual last-commit (`pushed_at`), confirmed by reading the repo.
</Callout>

| Module | Scope | Status (as of Sep 2026) | Input shape |
|---|---|---|---|
| [`databricks/terraform-databricks-examples`](https://github.com/databricks/terraform-databricks-examples) | Official, ~30 independent per-cloud folders | Active — commits within the last month | No unified schema. `aws-databricks-uc`'s metastore-assignment pattern (`databricks_workspace_ids = list(string)`) is the one cleanly-driveable exception |
| [`Azure/avm-res-databricks-workspace`](https://registry.terraform.io/modules/Azure/avm-res-databricks-workspace/azurerm/latest) | Microsoft's Azure Verified Module for a Databricks workspace | Active, MIT — commit days old, 116k+ downloads | Typed objects: `custom_parameters {...}`, `access_connector = map(object({...}))` |
| [`data-platform-hq/terraform-databricks-unity-catalog`](https://github.com/data-platform-hq/terraform-databricks-unity-catalog) | Catalog + schema + grants factory, one entry per catalog | Apache-2.0, ~19 months no commits — pin the version, plan-test first | `catalog_config = list(object({catalog_name, schema_configs, catalog_grants, …}))` — cleanest catalog-factory shape found |
| [`data-platform-hq/terraform-databricks-external-location`](https://github.com/data-platform-hq/terraform-databricks-external-location) | Storage credential + external locations, list-driven | Apache-2.0, same ~19-month staleness | `external_locations = list(object({name, url, credentials_name, permissions, …}))` |
| [`databricks/terraform-databricks-sra`](https://github.com/databricks/terraform-databricks-sra) | Per-cloud secure reference deployment | Active (218★) but README says **"exploration purposes only… not formally supported"** | Independent per-cloud `tfvars`, no shared schema |
| [`PerfectThymeTech/terraform-azurerm-data-landing-zone`](https://registry.terraform.io/modules/PerfectThymeTech/data-landing-zone/azurerm/latest) | One flat var-file drives an Azure data landing zone incl. Databricks | MIT, ~14 months stale, 20 open issues | One root `variables.tf` — closest to "one file drives everything," Azure-only |

<Callout client:visible variant="warning" title="Two traps: a project that looks alive, and a license that forbids the fix everyone assumes it allows">
  **`aztfmod/terraform-azurerm-caf`** (588★, MIT, the exact `for_each` factory shape most searches surface first) looks active from its GitHub feed — but `main` hasn't merged a commit since March 2025; newer timestamps are issue/PR/star events, not code.

  **`databricks/terraform-databricks-lakehouse-blueprints`** composes well-shaped modules — but its license states the source is Databricks Confidential Information and forbids derivative works without a separate agreement. Use only as published, unmodified.
</Callout>

None of these ship a schema dbxmig auto-generates today — [`dbxmig handoff`](/templates/automation-toolkit) covers the accelerators, not target-foundation Terraform. The `aws-databricks-uc` workspace-ID list and the `data-platform-hq` catalog-factory shape are the two worth automating if this becomes a repeated need.

## AWS target foundation

<CodeBlock client:visible language="hcl" filename="aws-target.tf" code={`terraform {
  required_providers {
    databricks = {
      source  = "databricks/databricks"
      version = ">= 1.36"
    }
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

provider "databricks" {
  alias    = "mws"
  host     = "https://accounts.cloud.databricks.com"
  account_id = var.databricks_account_id
  username = var.databricks_account_user
  password = var.databricks_account_password
}

resource "databricks_mws_vpc_endpoint" "backend" {
  provider                   = databricks.mws
  account_id                 = var.databricks_account_id
  aws_vpc_endpoint_id        = aws_vpc_endpoint.backend.id
  vpc_endpoint_name          = "\${var.prefix}-backend"
  region                     = var.region
  depends_on                 = [aws_vpc_endpoint.backend]
}

resource "databricks_storage_credential" "uc" {
  provider     = databricks.mws
  name         = "\${var.prefix}-uc-storage"
  metastore_id = databricks_metastore.this.id
  aws_iam_role {
    role_arn = aws_iam_role.uc_storage.arn
  }
}

resource "databricks_metastore" "this" {
  provider      = databricks.mws
  name          = "\${var.prefix}-metastore"
  storage_root  = "s3://\${aws_s3_bucket.metastore.id}/metastore"
  region        = var.region
  force_destroy = true
}`} />

## Azure target foundation

<CodeBlock client:visible language="hcl" filename="azure-target.tf" code={`resource "azurerm_databricks_access_connector" "uc" {
  name                = "\${var.prefix}-uc-connector"
  resource_group_name = azurerm_resource_group.this.name
  location            = var.region
  identity {
    type = "SystemAssigned"
  }
}

resource "azurerm_role_assignment" "uc_storage" {
  scope                = azurerm_storage_account.uc.id
  role_definition_name = "Storage Blob Data Contributor"
  principal_id         = azurerm_databricks_access_connector.uc.identity[0].principal_id
}

resource "databricks_metastore" "this" {
  name         = "\${var.prefix}-metastore"
  storage_root = "abfss://\${azurerm_storage_container.uc.name}@\${azurerm_storage_account.uc.name}.dfs.core.windows.net/metastore"
  region       = var.region
}

resource "databricks_metastore_data_access" "uc" {
  metastore_id = databricks_metastore.this.id
  name         = "\${var.prefix}-uc-access"
  azure_managed_identity {
    access_connector_id = azurerm_databricks_access_connector.uc.id
  }
  is_default = true
}`} />

## GCP target foundation

<CodeBlock client:visible language="hcl" filename="gcp-target.tf" code={`resource "google_service_account" "uc" {
  account_id   = "\${var.prefix}-uc-sa"
  display_name = "Databricks UC Storage"
}

resource "google_storage_bucket_iam_member" "uc" {
  bucket = google_storage_bucket.uc.name
  role   = "roles/storage.objectAdmin"
  member = "serviceAccount:\${google_service_account.uc.email}"
}

resource "google_service_account_iam_member" "databricks_token_creator" {
  service_account_id = google_service_account.uc.name
  role               = "roles/iam.serviceAccountTokenCreator"
  member             = "serviceAccount:\${var.databricks_google_service_account}"
}

resource "databricks_metastore" "this" {
  name         = "\${var.prefix}-metastore"
  storage_root = "gs://\${google_storage_bucket.uc.name}/metastore"
  region       = var.region
}`} />

<Callout client:visible variant="tip" title="Modularize">
  Keep cloud-specific modules separate and use a common variables interface. This makes multi-cloud migrations easier to maintain and review.
</Callout>

## Validation

- Run `terraform plan` against the target account and confirm zero unexpected diffs before every apply — especially after re-running the exporter, since re-imports can introduce drift in provider-managed fields.
- Verify the metastore, storage credential, and access connector resources actually resolve (test a `CREATE TABLE` and `GRANT` in the target) before treating the foundation as complete.

## Rollback

Target foundation resources created via Terraform should be destroyed via `terraform destroy` scoped to the specific module, not manually in the cloud console — manual deletion causes state drift that blocks future applies. Keep `terraform.tfstate` for the target foundation in remote state (not local disk) so rollback is possible even if the machine that ran `apply` is gone.

## Automation opportunity

- Run `terraform plan` in CI on every pull request touching the foundation modules, and require a human-reviewed `plan` output before `apply`.
- Store per-cloud variable files (`aws.tfvars`, `azure.tfvars`, `gcp.tfvars`) in version control so the same module set can be re-applied for future migrations without rediscovering variable names.

## Evidence to capture

- The `terraform plan` and `apply` output for the target foundation, archived alongside the migration's decision register.
- The exporter's output diff (source configuration vs. what was actually carried into the target module) so reviewers can see what was intentionally left out.

## Cloud-specific caveats

The `databricks` Terraform provider's resource names and required arguments differ meaningfully across clouds even where the concept is shared — `databricks_storage_credential` and `databricks_metastore_data_access` take an `aws_iam_role { role_arn = ... }` block on AWS, an `azure_managed_identity { access_connector_id = ... }` block on Azure (the Azure Databricks Access Connector, not a raw managed identity, is the actual UC storage-credential object), and a Databricks-managed `databricks_gcp_service_account` block on GCP — the three are not interchangeable, and provider schema for these resources has changed across releases. Always check the exact schema for your pinned provider version rather than assuming argument names transfer from the AWS example to Azure/GCP.

<Checklist client:visible title="Terraform target foundation" items={[
  { id: 'export', label: 'Source workspace configuration exported as a baseline (where a source workspace exists)' },
  { id: 'modules', label: 'Target foundation modules written per cloud with a shared variables interface' },
  { id: 'plan-review', label: 'terraform plan reviewed in CI before every apply' },
  { id: 'state', label: 'Remote state configured for the target foundation module' },
  { id: 'validate', label: 'Target metastore, storage credential, and connectivity validated post-apply' },
  { id: 'factory-staleness', label: 'Any third-party factory-pattern module checked for actual last-commit date, not the repo page\'s "updated" badge' },
  { id: 'factory-license', label: 'Module license confirmed to permit forking/modifying before adapting it, not just using it as published' },
]} />

## Sources

- Databricks Terraform exporter guide — [experimental-exporter.md](https://github.com/databricks/terraform-provider-databricks/blob/main/docs/guides/experimental-exporter.md) (`-services` scope, DAB-managed jobs excluded, `-incremental` limits, `-targetCloud`/`-nodeTypeMappingFile` coverage)
- Asset Bundle custom templates — [template-tutorial](https://docs.databricks.com/aws/en/dev-tools/bundles/template-tutorial) (`bundle init --config-file`)
- [databricks/terraform-databricks-examples](https://github.com/databricks/terraform-databricks-examples)
- [Azure/avm-res-databricks-workspace](https://registry.terraform.io/modules/Azure/avm-res-databricks-workspace/azurerm/latest)
- [data-platform-hq/terraform-databricks-unity-catalog](https://github.com/data-platform-hq/terraform-databricks-unity-catalog) · [terraform-databricks-external-location](https://github.com/data-platform-hq/terraform-databricks-external-location)
- [databricks/terraform-databricks-sra](https://github.com/databricks/terraform-databricks-sra)
- [PerfectThymeTech/terraform-azurerm-data-landing-zone](https://registry.terraform.io/modules/PerfectThymeTech/data-landing-zone/azurerm/latest)
- [aztfmod/terraform-azurerm-caf](https://github.com/aztfmod/terraform-azurerm-caf) — cited as a staleness cautionary example, not a recommendation
- [databricks/terraform-databricks-lakehouse-blueprints](https://github.com/databricks/terraform-databricks-lakehouse-blueprints) — cited for its license restriction, not as a recommended fork target
