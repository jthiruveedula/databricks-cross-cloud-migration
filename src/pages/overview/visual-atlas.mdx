---
title: 'Visual atlas'
layout: ../../layouts/Layout.astro
description: 'Interactive diagrams for the migration: architecture, data flow, runbook, event path, per-table state, all six cloud-pair topologies, and the discovery phase that scopes them.'
hideToc: true
---

import Callout from '../../components/Callout.tsx';
import Card from '../../components/Card.tsx';
import ArchifyDiagram from '../../components/ArchifyDiagram.tsx';
import { withBase } from '../../lib/paths';

# Visual atlas

Five views of the same migration, each answering a question the others cannot. The worked example throughout is **Azure → GCP**, but the shapes hold for any cross-cloud pair — swap ADLS Gen2 for S3, Pub/Sub for Kinesis, and the lanes, gates, and states are unchanged. All six cloud-pair architecture diagrams are at the bottom of this page.

Each diagram is interactive: switch light/dark, play the guided views, trace a route between two nodes, or export the current state as PNG, SVG, or WebM. Every diagram also appears inline on the page that owns its subject, linked below.

<Callout client:visible variant="tip" title="Read them in this order">
  Architecture answers *what exists*. Data flow answers *what moves*. The runbook answers *in what order*. The sequence answers *what a single event experiences*. The lifecycle answers *what happens when one table fails its check*. Skipping to the runbook without the first two is how teams end up sequencing work they have not scoped.
</Callout>

## 1. What sits on each side

<ArchifyDiagram
  client:visible
  name="azure-to-gcp.architecture"
  type="architecture"
  title="Estate lift-and-shift: source, migration control plane, target"
  caption="Four independent lanes — events, compute, storage, catalog. The middle column is the migration machinery itself, which is temporary and should be deleted after decommissioning."
/>

Owned by [Azure → GCP](/cloud-mappings/azure-to-gcp). The equivalent mapping for other cloud pairs is in the [cross-cloud equivalency matrix](/cloud-mappings/cross-cloud-equivalency-matrix).

## 2. What actually moves

<ArchifyDiagram
  client:visible
  name="table-migration.dataflow"
  type="dataflow"
  title="Table and event migration pipeline"
  caption="Objects, catalog metadata, and event traffic take three separate paths and finish at different times. A table is not migrated when its files land — it is migrated when its DDL, grants, and history are all present on the target."
/>

Owned by [large-scale data transfer](/execution/large-scale-data-transfer). Catalog specifics are in [metastore migration](/governance/metastore-migration) and [UCX strategy](/governance/ucx-strategy).

## 3. In what order

<ArchifyDiagram
  client:visible
  name="cutover-runbook.workflow"
  type="workflow"
  title="Cutover runbook, per wave"
  caption="Everything left of the freeze overlaps with production and is reversible. The reconciliation diff is a blocking gate; repointing consumers is the step a rollback cannot undo cheaply."
/>

Owned by [wave planning](/execution/wave-planning). The cutover-night handoffs between teams are in [cutover](/execution/cutover), and the reverse path is in [rollback](/execution/rollback).

## 4. What one event experiences

<ArchifyDiagram
  client:visible
  name="event-bridge.sequence"
  type="sequence"
  title="Event Hubs to Pub/Sub to Delta, one message end to end"
  caption="The streaming job acks only after the Delta commit, and redelivery past the ack deadline is absorbed by a MERGE on event id — the reason an at-least-once source can safely land in an exactly-once table."
/>

Owned by [DLT / Lakeflow pipelines](/pipelines/dlt-lakeflow), where the checkpoint-portability decision this diagram depends on is spelled out.

## 5. What happens when one table fails

<ArchifyDiagram
  client:visible
  name="table-state.lifecycle"
  type="lifecycle"
  title="Per-table migration lifecycle"
  caption="The wave is a set of per-table state machines, not one switch. A failed diff routes that table to Repairing and back through the gate; Failed is reserved for a table that cannot be reconciled inside the window."
/>

Owned by [data reconciliation](/validation/data-reconciliation). Mismatch triage is in the same page's *Handling mismatches* section.

## 6. Every cloud pair, same shape

The four-lane architecture above is not Azure→GCP-specific — it is the shape of a Databricks platform reset between *any* two clouds. Each pair below swaps in that cloud's own storage, events, identity, and transfer mechanism; the lanes, the migration-control-plane column, and the cutover-safety cards stay structurally identical. Open a pair inline on its own page, where the diagram sits beside the identity-federation table and path-rewrite examples it corresponds to.

<div className="grid gap-4 sm:grid-cols-2">
  <Card href={withBase('/cloud-mappings/azure-to-gcp')} title="Azure → GCP" description="ADLS Gen2 → GCS, Event Hubs → Pub/Sub, Entra ID → Google Cloud Identity." />
  <Card href={withBase('/cloud-mappings/aws-to-azure')} title="AWS → Azure" description="S3 → ADLS Gen2, EventBridge → Event Hubs, IAM Identity Center → Entra ID." />
  <Card href={withBase('/cloud-mappings/azure-to-aws')} title="Azure → AWS" description="ADLS Gen2 → S3, Event Hubs → EventBridge, Entra ID → IAM Identity Center." />
  <Card href={withBase('/cloud-mappings/aws-to-gcp')} title="AWS → GCP" description="S3 → GCS, EventBridge → Pub/Sub, IAM Identity Center → Google Cloud Identity." />
  <Card href={withBase('/cloud-mappings/gcp-to-azure')} title="GCP → Azure" description="GCS → ADLS Gen2, Pub/Sub → Event Hubs, Google Cloud Identity → Entra ID." />
  <Card href={withBase('/cloud-mappings/gcp-to-aws')} title="GCP → AWS" description="GCS → S3, Pub/Sub → EventBridge, Google Cloud Identity → IAM Identity Center." />
</div>

The full [cross-cloud equivalency matrix](/cloud-mappings/cross-cloud-equivalency-matrix) is the construct-by-construct reference these six diagrams are drawn from.

## 7. Discovery, before any of the above

None of the first five diagrams have anything to sequence until discovery produces it. Four more diagrams cover that phase — the fan-out that collects the estate, the graph that clusters it into candidate waves, the scoring that tiers it, and the pattern that automates the data-gathering slice without letting a model touch raw rows.

<div className="grid gap-4 sm:grid-cols-2">
  <Card href={withBase('/discovery/asset-inventory')} title="Asset inventory" description="Metastore plane and workspace plane, collected separately, cross-referenced on Day 3 into shared-dependency clusters." />
  <Card href={withBase('/discovery/dependency-mapping')} title="Dependency mapping" description="Lineage to a graph, weakly-connected components to candidate wave boundaries — with the two things a human still has to resolve." />
  <Card href={withBase('/discovery/risk-assessment')} title="Risk assessment" description="Seven weighted dimensions to a tier, with owner engagement deliberately inverted." />
  <Card href={withBase('/discovery/ai-assisted-assessment')} title="AI-assisted assessment" description="Deterministic Spark SQL for every section except the narrative, which only ever sees an aggregated report." />
</div>

## 8. Security, where the migration actually breaks

Six more diagrams cover the security work that's hardest to get right: the circular dependency in an AWS storage credential, secrets that don't migrate, serverless egress owned by a different team, the classic-plane network posture, identity federation's break-glass path, and the one decision every table's residency reduces to.

<div className="grid gap-4 sm:grid-cols-2">
  <Card href={withBase('/security/target-storage-credentials')} title="Target storage credentials" description="Three passes to break a circular dependency — the storage credential everything else in a metastore migration waits on." />
  <Card href={withBase('/security/secrets-and-kms')} title="Secrets and KMS" description="Map, decide, recreate, sequence — with the source-cloud credential and the vault-only scope that don't carry over." />
  <Card href={withBase('/security/serverless-networking')} title="Serverless networking" description="Four ordered steps, and the firewall allow-list step most likely to be owned by a team never told about the migration." />
  <Card href={withBase('/security/network-security')} title="Network security" description="The classic compute plane's posture, and why serverless deliberately isn't on the same diagram." />
  <Card href={withBase('/security/identity-federation')} title="Identity federation" description="SSO, then groups, then validate — with a break-glass admin enabled the whole way through." />
  <Card href={withBase('/security/data-residency')} title="Data residency" description="One decision, four outcomes — and provider-constrained isn't a blocked migration." />
</div>

## Editing these diagrams

The diagrams are generated, not hand-drawn. Each one is a typed JSON spec in `diagrams/`, rendered to a self-contained HTML file in `public/diagrams/` by [Archify](https://github.com/tt-a1i/archify):

```bash
npm run build:diagrams:check   # validate specs at showcase quality
npm run build:diagrams         # re-render every diagram
```

Edit the spec and re-render; never hand-edit the HTML. The rendered files are committed so that CI and the Pages deploy never depend on Archify being installed. Full contributor notes are in `CONTRIBUTING.md`.
