---
title: 'Grants and roles'
layout: ../../layouts/Layout.astro
description: 'Replicate Unity Catalog grants and role assignments in the target metastore.'
---

import Callout from '../../components/Callout.tsx';
import CodeBlock from '../../components/CodeBlock.tsx';
import Checklist from '../../components/Checklist.tsx';
import ArchifyDiagram from '../../components/ArchifyDiagram.tsx';

# Grants and roles

Creating the target metastore, catalogs, and schemas from [Unity Catalog strategy](/governance/unity-catalog-strategy) only reproduces structure. Grants are what make data usable — and they are the part most teams under-budget time for, because there is no bulk "copy grants" operation across metastores. Grants control access to the object as a whole; for row filters, column masks, and tag-based ABAC policies that restrict access *inside* a table, see [row and column-level security](/governance/row-column-security).

<Callout client:visible variant="warning" title="Grants do not travel with data">
  Cloning a table with Delta Deep Clone copies data and history, not Unity Catalog grants. Every `GRANT` statement on the source metastore must be re-issued explicitly against the target — plan for this as its own migration step, not an assumed side effect of data replication.
</Callout>

## Inventory source grants first

Before writing a single target `GRANT`, extract the full source grant set — do not reconstruct it from memory or tribal knowledge:

<CodeBlock client:visible language="sql" filename="inventory_grants.sql" code={`-- Run against the source metastore for every catalog in scope
SHOW GRANTS ON CATALOG source_catalog;
SHOW GRANTS ON SCHEMA source_catalog.sales;
SHOW GRANTS ON TABLE source_catalog.sales.orders;

-- Or query the information_schema for a full, exportable inventory. There is
-- no single "grants" view -- privileges live in one view per securable type,
-- so a complete export unions them.
SELECT 'CATALOG' AS object_type, catalog_name AS object_name, grantee, privilege_type
FROM system.information_schema.catalog_privileges
WHERE catalog_name = 'source_catalog'
UNION ALL
SELECT 'SCHEMA', concat_ws('.', catalog_name, schema_name), grantee, privilege_type
FROM system.information_schema.schema_privileges
WHERE catalog_name = 'source_catalog'
UNION ALL
SELECT 'TABLE', concat_ws('.', table_catalog, table_schema, table_name), grantee, privilege_type
FROM system.information_schema.table_privileges
WHERE table_catalog = 'source_catalog'
UNION ALL
SELECT 'VOLUME', concat_ws('.', volume_catalog, volume_schema, volume_name), grantee, privilege_type
FROM system.information_schema.volume_privileges
WHERE volume_catalog = 'source_catalog'
UNION ALL
SELECT 'FUNCTION', concat_ws('.', specific_catalog, specific_schema, specific_name),
       grantee, privilege_type
FROM system.information_schema.routine_privileges
WHERE specific_catalog = 'source_catalog';
`} />

<Callout client:visible variant="warning" title="Run the export as a metastore admin, or you will export a subset and not know it">
  `information_schema` **automatically filters** its results to the objects the querying principal has privileges on. A data engineer running this export gets a clean-looking result containing only the grants they can already see, migrates it, and finds the gaps when other teams start filing access tickets weeks later. Sanity-check the exported grant count against the object count before trusting it. (`MANAGE` on an object is also not currently sufficient to see all of that object's grants here.)
</Callout>

<ArchifyDiagram
  client:visible
  name="grants-and-roles.workflow"
  type="workflow"
  title="Export, translate, apply top-down, verify"
  caption="A non-admin export is silently filtered to what that principal can already see. Grant catalog, then schema, then table — the wrong order fails with confusing permission errors."
/>

## Grant migration pattern

| Step | Action |
|------|--------|
| 1 | Export source grants via the `*_PRIVILEGES` information_schema views or `SHOW GRANTS`, as a metastore admin |
| 2 | Translate source principal names to target account-level group names |
| 3 | Generate target `GRANT` statements from the translated mapping |
| 4 | Apply grants at catalog/schema level first, table-level exceptions second |
| 5 | Diff target grants against the source export — `dbxmig verify -i source.json -t target.json` |

<p className="text-xs text-[var(--ink-subtle)]">Illustrative — adapt the CSV schema and error handling to your export format before running against production.</p>

<CodeBlock client:visible language="python" filename="generate_grants.py" code={`import csv
import json

PRINCIPAL_MAP = json.load(open("principal_mapping.json"))  # source group -> target group

def translate_grant(row: dict) -> str:
    target_principal = PRINCIPAL_MAP.get(row["grantee"])
    if not target_principal:
        raise KeyError(f"No target mapping for principal: {row['grantee']}")
    return f"GRANT {row['privilege_type']} ON {row['object_type']} {row['object_name']} TO \`{target_principal}\`;"

# source_grants.csv is the export from inventory_grants.sql above
with open("source_grants.csv") as f:
    source_grants = list(csv.DictReader(f))

statements = [translate_grant(r) for r in source_grants]
`} />

## Migration is a re-baseline opportunity, not just a replay

Every step above replays the source grant set faithfully — which also faithfully replays any over-permissioning that accumulated in source over the years. A 1:1 replay is the right *starting* target (it's the only way to guarantee nothing legitimate breaks), but treating it as the *final* state means the migration shipped every stale grant along with the real ones.

<Callout client:visible variant="tip" title="Re-baseline after replay, not instead of it">
  Don't try to redesign least-privilege as part of the migration itself — that turns a migration into a governance redesign project and puts both at risk. Replay 1:1 first, prove nothing broke, then run a **separate**, post-hypercare re-baseline pass with its own sign-off. The migration's job is parity; the re-baseline's job is improvement, and conflating them is how both take longer and neither finishes.
</Callout>

Add a negative-privilege check alongside the positive one this page already recommends:

<CodeBlock client:visible language="sql" filename="unused_grants.sql" code={`-- Grants that exist but were never exercised in the observation window --
-- candidates for the post-hypercare re-baseline, not proof of a problem on
-- their own (a quarterly report a principal hasn't needed yet is not "unused").
SELECT p.grantee, p.object_type, p.object_name, p.privilege_type
FROM system.information_schema.table_privileges p
LEFT JOIN (
  SELECT DISTINCT user_identity.email AS principal, request_params.full_name_arg AS object_name
  FROM system.access.audit
  WHERE event_date >= current_date() - INTERVAL 90 DAYS
    AND action_name IN ('getTable', 'getTableV2')
) used ON p.grantee = used.principal AND p.object_name = used.object_name
WHERE used.principal IS NULL;`} />

<Callout client:visible variant="warning" title="Unexercised does not mean unnecessary">
  This query surfaces candidates for review, not a delete list. A grant nobody has used in 90 days might back an annual close process, a disaster-recovery runbook, or a report someone runs once a quarter. Route every result to the object owner for a decision — keep, document why it's needed, or revoke — rather than acting on the query output directly.
</Callout>

## Role hierarchy

- Grant at the **catalog** or **schema** level by default; reserve **table-level** grants for genuine exceptions — table-by-table grants are the ones that get missed during migration and drift afterward.
- Use Unity Catalog groups, not individual users, as grantees everywhere possible — it's the difference between updating one group membership and rewriting grants when someone changes teams.
- Keep a `data-engineering-admin`-style ownership group per domain, separate from the broader read-access group, so ownership transfer doesn't require re-granting every privilege.

<Callout client:visible variant="tip" title="Grant catalogs and schemas before tables">
  If you grant table-level access before the schema exists, or in the wrong order, some Unity Catalog operations fail with confusing permission errors. Apply grants top-down: catalog → schema → table/view/volume.
</Callout>

## Validation

- Diff `SHOW GRANTS` output between source and target for every catalog — do not sample; a missed grant surfaces as a support ticket weeks after cutover, not during testing.
- Have the owning team run their actual queries/dashboards against the target with their real (non-admin) credentials — an admin token testing access proves nothing about a regular user's grants.
- Confirm service principals used by jobs and CI/CD have the grants they need, not just interactive users.

## Rollback

Grants are additive and low-risk to fix forward — if a grant is missing or wrong, correct it directly in the target metastore. There is no need to roll back data for a grants issue; keep source grants untouched as the reference until hypercare ends.

## Automation opportunity

- Store the full grant set as declarative Terraform (`databricks_grants` resource) per catalog/schema so drift is visible in a `terraform plan`, not discovered by a user losing access.
- Run the source-vs-target grant diff as a scheduled check throughout hypercare, not just once at cutover.

<Checklist client:visible title="Grants and roles migration" items={[
  { id: 'inventory', label: 'Full source grant inventory extracted' },
  { id: 'mapping', label: 'Source principals mapped to target account-level groups' },
  { id: 'apply', label: 'Grants applied top-down: catalog → schema → table' },
  { id: 'diff', label: 'Target grants diffed against source export with zero gaps' },
  { id: 'real-user-test', label: 'Access validated with real (non-admin) user credentials' },
  { id: 'rebaseline-scheduled', label: 'Post-hypercare least-privilege re-baseline scheduled, with its own owner and sign-off — separate from the migration itself' },
]} />

## Sources

- Databricks — [Information schema](https://docs.databricks.com/aws/en/sql/language-manual/sql-ref-information-schema) (the full view list; there is no single `grants` view, and results are automatically filtered by the caller's privileges)
- Databricks — [`TABLE_PRIVILEGES`](https://docs.databricks.com/aws/en/sql/language-manual/information-schema/table_privileges), [`SCHEMA_PRIVILEGES`](https://docs.databricks.com/aws/en/sql/language-manual/information-schema/schema_privileges), [`CATALOG_PRIVILEGES`](https://docs.databricks.com/aws/en/sql/language-manual/information-schema/catalog_privileges)
